# AUDIT iOS (LocationHistory2GPX-iOS) – 2026-03-31_08-16

## 1. Ziel / Scope

Repo-Truth-Pruefung des iOS-Consumer-Repos als Teil des 4-Repo-Audits.

## 2. Gelesene Pflichtdateien

README.md, CHANGELOG.md, NEXT_STEPS.md, AGENTS.md, ROADMAP.md, Package.swift, docs/CONTRACT.md, docs/APP_FEATURE_INVENTORY.md, docs/APPLE_VERIFICATION_CHECKLIST.md, docs/XCODE_APP_PREPARATION.md, docs/XCODE_RUNBOOK.md

## 3. Pflichtdateien nicht vorhanden

Keine fehlenden Pflichtdateien identifiziert.

## 4. Zusaetzlich entdeckte relevante Dateien

Package.resolved, .github/workflows/swift-test.yml, scripts/update_contract_fixtures.sh, scripts/run_app_shell_macos.sh, .swiftlint.yml, Fixtures/contract/* (16 JSON-Fixtures), Sources/* (ca. 70+ Swift-Dateien ueber 6 Targets), Tests/* (ca. 40+ Testdateien)

## 5. Bewusst nicht vertiefte Dateien

Einzelne Swift-Source-Dateien (Scope ist Doku-Wahrheit, nicht Code-Review). Backup-Dateien (*__backup_2026-03-31_04-59.md).

## 6. Ausgefuehrte Pruefungen

- `swift test`: 228 Tests, 2 Skips, 0 Failures (frisch auf diesem Linux-Host)
- `git diff --check`: sauber
- `which xcodebuild`: nicht vorhanden – auf diesem Linux-Host nicht verifiziert

## 7. Gefundene Widersprueche

Keine neuen Widersprueche gefunden. Das 04-59-Audit hatte die Doku bereits synchronisiert.

## 8. Gefundene veraltete Aussagen

Keine neuen veralteten Aussagen gefunden.

## 9. Gefundene fehlende Doku

Keine fehlende Doku identifiziert.

## 10. Gefundene unbelegte Claims

Keine unbelegten Claims gefunden. Historische Apple-Nachweise sind korrekt als historisch markiert.

## 11. Nicht dokumentierte Implementierungen

Hardcoded Test-Server-IP in `LiveLocationServerUploader.swift` Zeile 7: `https://178-104-51-78.sslip.io/live-location`. Ist in Wrapper-README und receiver OPEN_ITEMS.md als offener Punkt dokumentiert, aber nicht explizit in diesem Repo als offener Bereinigungspunkt gefuehrt. Wird im 4-Repo-Master-Audit als Cross-Repo-Risiko gefuehrt.

## 12. Konkrete Korrekturen

Keine inhaltlichen Korrekturen noetig. Nur neue Audit-Dateien angelegt.

## 13. Verbleibende offene Punkte

- Frischer Apple-CLI-Gegenlauf (Phase 19.53)
- Apple-UI-Verifikation fuer Live/Upload/Insights/Days/Heatmap (Phase 19.51/19.52)
- Background-Recording Device-Verifikation (Phase 19.54)
- Wrapper-Auto-Restore Re-Verifikation (Phase 19.55)
- Server-Upload/Review/Privacy Finalisierung (Phase 19.56)
- Hardcoded Test-Server-IP bereinigen (siehe OPEN_ITEMS im Receiver-Repo)

## 14. Ehrliche Grenzen der Verifikation

- Linux-Host ohne `xcodebuild`: Kein Apple-Build, kein Simulator, kein Device-Test.
- `swift test` auf Linux verifiziert nur non-UI-Code. Apple-only Tests (2 Skips) sind korrekt gegated.

## 15. Abschlussfazit

Das iOS-Repo ist nach dem 04-59-Audit bereits konsistent. Keine inhaltlichen Aenderungen noetig. Die 228 Tests laufen frisch auf diesem Linux-Host gruen. Der einzige neu identifizierte Cross-Repo-Punkt (hardcoded Test-Server-IP) war bereits als offener Punkt dokumentiert.
