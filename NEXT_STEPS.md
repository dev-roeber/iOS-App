# NEXT_STEPS

1. Die jetzt dokumentierte Apple-UI-Verifikation bei echten macOS-/Xcode-Aenderungen nur gezielt regressionspruefen, statt sie erneut nur hypothetisch zu beschreiben.
2. Falls der genaue IDE-Lauf `Product > Run` spaeter regressionskritisch wird, diesen foreground-Pfad separat gegen die bestehende Checkliste nachziehen.
3. Contract-Files weiter ausschliesslich vom Producer-Repo aus aktualisieren.
