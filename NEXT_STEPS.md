# NEXT_STEPS

1. Die noch offene interaktive Apple-Verifikation fuer `LocationHistoryConsumerApp` in Xcode abschliessen: sichtbarer App-Start, `Load Demo Data`, `Open app_export.json`, `Clear`, invalides JSON und leerer Export gemaess `docs/APPLE_VERIFICATION_CHECKLIST.md`.
2. Nur wenn dabei echte Apple-Laufprobleme sichtbar werden, kleine package- oder app-shell-nahe Klarstellungen nachziehen, ohne neuen Feature-Scope zu oeffnen.
3. Contract-Files weiter ausschliesslich vom Producer-Repo aus aktualisieren.
