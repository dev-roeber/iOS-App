# NEXT_STEPS

1. Falls der genaue IDE-Lauf `Product > Run` spaeter regressionskritisch wird, diesen foreground-Pfad separat gegen die bestehende Checkliste nachziehen.
2. Contract-Files weiter ausschliesslich vom Producer-Repo aus aktualisieren.
3. Bei echten macOS-/Xcode-Aenderungen die Apple-UI-Verifikation nur gezielt regressionspruefen, nicht erneut hypothetisch beschreiben.
